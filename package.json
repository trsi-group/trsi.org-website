{
  "name": "trsi.org-website",
  "version": "1.0.0",
  "description": "TRSI Website",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fetch-content": "node scripts/fetchContent.js",
    "transform-content": "node scripts/transformContent.js",
    "copy-content": "node scripts/copyContent.js",
    "build": "npm run fetch-content && npm run transform-content && npm run copy-content"
  },
  "author": "TRSI",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.3",
    "contentful-cli": "^3.7.3"
  }
}
